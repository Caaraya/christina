<%
page = ""
controller.poems.each do |item|
	page += simple_format("#{item.title}\n")

	page += simple_format("#{item.author}\n")
	
	page += simple_format(link_to('Read',
	'poems/' + item.id.to_s + '/show/'))
end
%>

<%= simple_format(page)%>